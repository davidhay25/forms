angular.module("formsApp")

    .service('designerSvc', function($q,$http,$filter,moment) {

        return {
            //generate the treedata for a LM
            //the parent of an item:
            // is the
            "makeLMTree" : function(Q) {



                 return LM
            }
        }

    })